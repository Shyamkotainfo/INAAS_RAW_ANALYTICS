# Layer 2 — Profile / Statistics Store
# Serves pre-computed stats: null counts, distinct counts, distributions, top-N values.
# PySpark ran once as a profiling job; results are read here at query time.
